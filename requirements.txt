# Sigma Analyst - AI Financial Analysis System with Vision Stack
# Updated: 2025-11 - Python 3.12 with NumPy 2.x ecosystem
# Windows & Linux compatible with environment markers
# Successfully tested on Windows 11 with Python 3.12.3 + RTX 4060

# ============================================================================
# INSTALLATION GUIDE (Windows 11 + Python 3.12.3 + RTX 4060)
# ============================================================================
#
# Step 1: Upgrade pip, setuptools, wheel
#   python -m pip install -U pip setuptools wheel
#
# Step 2: Install PyTorch with CUDA (CRITICAL - DO THIS FIRST!)
#   ⚠️  This MUST be done BEFORE installing requirements.txt!
#
#   For CUDA 12.4 (RTX 4060 recommended):
#     python -m pip uninstall -y torch torchvision torchaudio
#     python -m pip cache purge
#     python -m pip install --index-url https://download.pytorch.org/whl/cu124 --no-cache-dir --force-reinstall torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1
#
#   For CUDA 12.1:
#     python -m pip install --index-url https://download.pytorch.org/whl/cu121 --no-cache-dir --force-reinstall torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1
#
#   Verify GPU support:
#     python -c "import torch; print(f'CUDA: {torch.cuda.is_available()}, GPU: {torch.cuda.get_device_name(0) if torch.cuda.is_available() else None}')"
#
#   Expected output:
#     CUDA: True, GPU: NVIDIA GeForce RTX 4060
#
# Step 3: Lock NumPy 2.x stack
#   python -m pip install numpy==2.2.6 pandas==2.3.3 scipy==1.14.1
#
# Step 4: Install requirements.txt (PyTorch already installed, will be skipped)
#   python -m pip install -r requirements.txt
#
# Step 5: (Optional) TA-Lib for advanced indicators
#   Download wheel from: https://github.com/cgohlke/talib-build/releases
#   python -m pip install ta_lib-0.6.8-cp312-cp312-win_amd64.whl
#
# Step 6: (Linux only) detectron2 for advanced layout analysis
#   pip install 'git+https://github.com/facebookresearch/detectron2.git'
#
# ============================================================================

# ===== CORE WEB =====
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17
websockets==14.1

# ===== NUMERIC / DATA =====
# NumPy 2.x ecosystem (modern, performant)
numpy==2.2.6
pandas==2.3.3
scipy==1.14.1
pyarrow==18.1.0

# ===== ML (Traditional) =====
scikit-learn==1.5.2
xgboost==2.1.2
lightgbm==4.5.0
catboost==1.2.8        # NumPy 2 support

# ===== DEEP LEARNING (PyTorch) =====
# CRITICAL: PyTorch MUST be installed FIRST with CUDA support!
# DO NOT install from this file - it will install CPU version!
#
# For RTX 4060 (CUDA 12.4):
#   pip install --index-url https://download.pytorch.org/whl/cu124 --no-cache-dir --force-reinstall torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1
#
# For CUDA 12.1:
#   pip install --index-url https://download.pytorch.org/whl/cu121 --no-cache-dir --force-reinstall torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1
#
# Verify GPU support after installation:
#   python -c "import torch; print(f'CUDA: {torch.cuda.is_available()}, GPU: {torch.cuda.get_device_name(0) if torch.cuda.is_available() else None}')"
#
# Uncomment ONLY if you want CPU version (NOT recommended for this project):
# torch==2.5.1
# torchvision==0.20.1
# torchaudio==2.5.1

# Transformers & Vision
transformers==4.46.3
accelerate==1.1.1
einops>=0.7.0
sentencepiece>=0.2.0

# Quantization support (Windows: may require manual compilation or skip)
bitsandbytes>=0.43.0 ; sys_platform != 'win32'

# ===== RL (Reinforcement Learning) =====
stable-baselines3==2.5.0  # NumPy 2 support
gymnasium==1.0.0
tensorboard==2.18.0

# ===== VISION / MULTI-MODAL =====
# YOLOv8 for object detection
ultralytics>=8.2.0
opencv-python>=4.10.0

# OCR (Windows: PaddleOCR can be problematic, consider rapidocr-onnxruntime instead)
paddleocr>=2.7.0 ; sys_platform != 'win32'
paddlepaddle>=2.6.0 ; sys_platform != 'win32' and python_version < '3.13'

# Layout Analysis (detectron2 requires Linux + CUDA dev tools, install separately)
layoutparser>=0.3.4
# detectron2 @ git+https://github.com/facebookresearch/detectron2.git  # Linux only - install manually

# PDF Processing
pdf2image>=1.17.0
pypdf2==3.0.1
pillow>=10.4.0

# ===== VISUALIZATION / CHARTING =====
matplotlib>=3.9.0
mplfinance==0.12.10b0  # Beta version (stable 0.12.10 not available on PyPI)
plotly>=5.24.0

# ===== TECHNICAL ANALYSIS =====
pandas-ta==0.4.71b0       # Requires NumPy >= 2.2.6
# TA-Lib for Windows: Download wheel from https://github.com/cgohlke/talib-build/releases
# Python 3.12: pip install ta_lib-0.6.8-cp312-cp312-win_amd64.whl
# Then uncomment below:
# ta-lib==0.6.8

# ===== EXCHANGES / I/O =====
ccxt==4.4.29
python-binance==1.0.22
requests==2.32.3
aiohttp==3.11.10
websocket-client==1.8.0

# ===== STORAGE / DB / QUEUE =====
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
redis==5.2.0
asyncpg==0.30.0
celery==5.4.0
flower==2.0.1
kombu==5.4.2

# ===== LLM / INTEGRATION =====
openai==1.55.3
anthropic==0.39.0
pinecone-client==5.0.1
langchain==0.3.11
langchain-community==0.3.11

# ===== CONFIG / UTILS =====
python-dotenv==1.0.1
pydantic==2.10.3
pydantic-settings==2.6.1
pyyaml==6.0.2
python-json-logger==3.2.1
prometheus-client==0.21.0
click==8.1.7
pytz==2024.2
httpx==0.28.1

# ===== DEV / TEST =====
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
black==24.10.0
isort==5.13.2
flake8==7.1.1
mypy==1.13.0
