# Sigma Analyst - AI Financial Analysis System
# Updated: 2025-10 - Python 3.12 with NumPy 2.x ecosystem
# Successfully tested on Windows with Python 3.12.3

# ===== CORE WEB =====
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17
websockets==14.1

# ===== NUMERIC / DATA =====
# NumPy 2.x ecosystem (modern, performant)
numpy==2.2.6
pandas==2.3.3
scipy==1.14.1
pyarrow==18.1.0

# ===== ML =====
scikit-learn==1.5.2
xgboost==2.1.2
lightgbm==4.5.0
catboost==1.2.8        # NumPy 2 support

# PyTorch with CUDA support (installed separately in Dockerfile)
# torch==2.5.1 (installed via --index-url cu121 in Dockerfile)
# torchvision==0.20.1 (installed via --index-url cu121 in Dockerfile)
# torchaudio==2.5.1 (installed via --index-url cu121 in Dockerfile)
transformers==4.46.3
accelerate==1.1.1

# ===== RL =====
stable-baselines3==2.5.0  # NumPy 2 support
gymnasium==0.29.1  # Compatible with SB3 2.5.0 (requires <0.30)
tensorboard==2.18.0

# ===== TECHNICAL ANALYSIS =====
pandas-ta==0.4.71b0       # Requires NumPy >= 2.2.6
TA-Lib==0.6.8             # NumPy 2.x compatible version (C library installed in Dockerfile)

# ===== VISUALIZATION & UTILS =====
matplotlib==3.9.2  # Fixed: 3.9.1 is yanked
tqdm==4.67.1  # Required by pandas-ta 0.4.71b0 (min: tqdm>=4.67.1)

# ===== EXCHANGES / I/O =====
ccxt==4.4.29
python-binance==1.0.22
requests==2.32.3
aiohttp==3.11.10
websocket-client==1.8.0

# ===== STORAGE / DB / QUEUE =====
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
redis==5.2.0
asyncpg==0.30.0
celery==5.4.0
flower==2.0.1
kombu==5.4.2

# ===== LLM / INTEGRATION =====
openai==1.55.3
anthropic==0.39.0
pinecone-client==5.0.1
langchain==0.3.11
langchain-community==0.3.11
pypdf2==3.0.1
PyMuPDF==1.24.5  # For PDF reading
# paddleocr==2.7.3  # OCR for scanned PDFs (heavy, install if needed)
# paddlepaddle==2.6.1  # OCR backend (heavy, install if needed)

# ===== CONFIG / UTILS =====
python-dotenv==1.0.1
pydantic==2.10.3
pydantic-settings==2.6.1
pyyaml==6.0.2
python-json-logger==3.2.1
prometheus-client==0.21.0
click==8.1.7
pytz==2024.2
httpx==0.28.1

# ===== DEV / TEST =====
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
black==24.10.0
isort==5.13.2
flake8==7.1.1
mypy==1.13.0
