# Sigma Analyst - AI Financial Analysis System with Vision Stack
# Updated: 2025-11 - Python 3.12 with NumPy 2.x ecosystem
# Windows & Linux compatible with environment markers
# Successfully tested on Windows 11 with Python 3.12.3 + RTX 4060

# ============================================================================
# INSTALLATION NOTES (Windows)
# ============================================================================
# 1) Install PyTorch with CUDA first (REQUIRED for GPU):
#    pip install --index-url https://download.pytorch.org/whl/cu124 torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1
#
# 2) Install core numeric stack to lock NumPy:
#    pip install numpy==2.2.6 pandas==2.3.3 scipy==1.14.1
#
# 3) Install this requirements file:
#    pip install -r requirements.txt
#
# 4) TA-Lib (optional, for advanced indicators):
#    Download wheel from https://github.com/cgohlke/talib-build/releases
#    pip install ta_lib-0.6.8-cp312-cp312-win_amd64.whl
#
# 5) Linux-only packages (detectron2) should be installed separately on Linux:
#    pip install 'git+https://github.com/facebookresearch/detectron2.git'
#
# ============================================================================

# ===== CORE WEB =====
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17
websockets==14.1

# ===== NUMERIC / DATA =====
# NumPy 2.x ecosystem (modern, performant)
numpy==2.2.6
pandas==2.3.3
scipy==1.14.1
pyarrow==18.1.0

# ===== ML (Traditional) =====
scikit-learn==1.5.2
xgboost==2.1.2
lightgbm==4.5.0
catboost==1.2.8        # NumPy 2 support

# ===== DEEP LEARNING (PyTorch) =====
# IMPORTANT: Install PyTorch with CUDA toolkit FIRST (see installation notes above)
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1
transformers==4.46.3
accelerate==1.1.1
einops>=0.7.0
sentencepiece>=0.2.0

# Quantization support (Windows: may require manual compilation or skip)
bitsandbytes>=0.43.0 ; sys_platform != 'win32'

# ===== RL (Reinforcement Learning) =====
stable-baselines3==2.5.0  # NumPy 2 support
gymnasium==1.0.0
tensorboard==2.18.0

# ===== VISION / MULTI-MODAL =====
# YOLOv8 for object detection
ultralytics>=8.2.0
opencv-python>=4.10.0

# OCR (Windows: PaddleOCR can be problematic, consider rapidocr-onnxruntime instead)
paddleocr>=2.7.0 ; sys_platform != 'win32'
paddlepaddle>=2.6.0 ; sys_platform != 'win32' and python_version < '3.13'

# Layout Analysis (detectron2 requires Linux + CUDA dev tools, install separately)
layoutparser>=0.3.4
# detectron2 @ git+https://github.com/facebookresearch/detectron2.git  # Linux only - install manually

# PDF Processing
pdf2image>=1.17.0
pypdf2==3.0.1
pillow>=10.4.0

# ===== VISUALIZATION / CHARTING =====
matplotlib>=3.9.0
mplfinance>=0.12.10
plotly>=5.24.0

# ===== TECHNICAL ANALYSIS =====
pandas-ta==0.4.71b0       # Requires NumPy >= 2.2.6
# TA-Lib for Windows: Download wheel from https://github.com/cgohlke/talib-build/releases
# Python 3.12: pip install ta_lib-0.6.8-cp312-cp312-win_amd64.whl
# Then uncomment below:
# ta-lib==0.6.8

# ===== EXCHANGES / I/O =====
ccxt==4.4.29
python-binance==1.0.22
requests==2.32.3
aiohttp==3.11.10
websocket-client==1.8.0

# ===== STORAGE / DB / QUEUE =====
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
redis==5.2.0
asyncpg==0.30.0
celery==5.4.0
flower==2.0.1
kombu==5.4.2

# ===== LLM / INTEGRATION =====
openai==1.55.3
anthropic==0.39.0
pinecone-client==5.0.1
langchain==0.3.11
langchain-community==0.3.11

# ===== CONFIG / UTILS =====
python-dotenv==1.0.1
pydantic==2.10.3
pydantic-settings==2.6.1
pyyaml==6.0.2
python-json-logger==3.2.1
prometheus-client==0.21.0
click==8.1.7
pytz==2024.2
httpx==0.28.1

# ===== DEV / TEST =====
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
black==24.10.0
isort==5.13.2
flake8==7.1.1
mypy==1.13.0
